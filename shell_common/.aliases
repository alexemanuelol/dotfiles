#!/bin/sh

alias ls='eza --icons=auto --color=auto --group-directories-first'
alias ll='eza -lahB --git --git-repos --icons=auto --color=auto --group-directories-first'

alias cd..="cd .."
alias ..="cd .."
alias ...="cd ../.."
alias ....="cd ../../.."
alias .....="cd ../../../.."

# Shortcuts
alias v="vim"
alias vi="vim"
alias g="git"

# Grep recursive, line number and no binaries
alias gr="grep -rnI"

# Search through command history
alias hs="history | grep"

# Work
alias nug="nuget install packages.config -OutputDirectory packages -ExcludeVersion"
alias r="rake"
