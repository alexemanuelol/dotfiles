---
extends: default

rules:
  indentation:
    spaces: 2
    indent-sequences: consistent
  line-length:
    max: 120
  truthy:
    allowed-values:
      - "yes"
      - "no"
      - "true"
      - "false"
